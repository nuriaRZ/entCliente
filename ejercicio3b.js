function ejercicio3b(){

    document.addEventListener("click", e=>{
        if(e.target.matches("#insertar")){

            let textoParrafo=document.getElementById("parrafo").value;
            let posicion = parseInt(document.getElementById("posicion").value);

            if(textoParrafo="")
                alert("no has introducido texto");
                else
                    if((posicion || posicion > 10 || isNaN.posicion)){
                        alerrt("posicion no valida, introduzca una nueva posicion"):
                    }else{
                        var lista=document.querySelector()
                    }
                        /**Lo mismo ya que antes */
                }
    }
}         



